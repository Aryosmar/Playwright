{
  "scripts": {
    "bdd:gen": "npx playwright-bdd",
    "test": "npm run bdd:gen && npx playwright test",
    "test:ui": "npm run bdd:gen && npx playwright test --ui",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "playwright-bdd": "^8.4.2"
  }
}
